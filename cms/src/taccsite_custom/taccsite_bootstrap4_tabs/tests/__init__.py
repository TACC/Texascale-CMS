"""Test package for taccsite_bootstrap4_tabs.

Placing test modules under the `tests` package ensures Django's test
discovery finds them when running `manage.py test <app>`.
"""

# This package intentionally left minimal.
